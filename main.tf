/*
This is a generic template for a Terraform module.
Add your resources here.

Example:
resource "aws_s3_bucket" "example" {
  bucket = "my-template-bucket-${var.name}" # Assuming a 'name' variable exists or is derived
  tags   = var.tags
}
*/